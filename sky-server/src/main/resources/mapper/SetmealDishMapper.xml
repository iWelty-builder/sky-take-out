<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.sky.mapper.SetmealDishMapper">
    <delete id="deletesetmeal">
        delete from sky_take_out.setmeal_dish  where setmeal_id=#{setmealId}
    </delete>
    <select id="setmealGetById" resultType="com.sky.entity.SetmealDish">
        select setmeal_id,dish_id,name,price,copies from sky_take_out.setmeal_dish where setmeal_id =#{setmealId}
    </select>

    <select id="getSetmealIdsByDishIds" resultType="java.lang.Long">
        SELECT setmeal_id
        FROM setmeal_dish
        WHERE dish_id IN
        <foreach item="dishId" collection="dishIds" open="(" close=")" separator=",">
            #{dishId}
        </foreach>
    </select>


    <insert id="setmealadd">
        INSERT INTO setmeal_dish (setmeal_id, dish_id, name, price, copies)
        SELECT t.* FROM (
        <foreach collection="setmealDish" item="smd" separator=" UNION ALL ">
            SELECT #{smd.setmealId} as setmeal_id, #{smd.dishId} as dish_id, #{smd.name} as name, #{smd.price} as price, #{smd.copies} as copies
        </foreach>
        <if test="setmealDish == null or setmealDish.size() == 0">
            SELECT NULL as setmeal_id, NULL as dish_id, NULL as name, NULL as price, NULL as copies FROM DUAL WHERE 1=0
        </if>
        ) t
    </insert>
</mapper>
